# Clang-format style for mini-co. LLVM base, tuned to match existing code
# (4-space indent, `void* x` pointers, 100-column limit) to keep the
# first format run a minimal diff.
BasedOnStyle: LLVM
IndentWidth: 4
ColumnLimit: 100
PointerAlignment: Left
DerivePointerAlignment: false
